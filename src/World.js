class World {
  constructor() {
    // this.height = FLOOR_HEIGHT;
  }

  draw() {
    // Floor
    // push();
    // fill(50);
    // rect(0, windowHeight - this.height, windowWidth, this.height)
    // pop();
  }
}

export default World;