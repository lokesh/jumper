const GRID_SIZE = 16;
const COLS = 30;
const ROWS = 20;

export default {
  GRID_SIZE,
  COLS,
  ROWS,

  CANVAS_WIDTH: COLS * GRID_SIZE,
  CANVAS_HEIGHT: ROWS * GRID_SIZE,

  PLAYER_W: 16,
  PLAYER_H: 32,
};
